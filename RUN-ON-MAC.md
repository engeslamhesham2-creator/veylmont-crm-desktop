# 🍎 تشغيل Veylmont CRM على الماك — خطوة بخطوة

الملف ده بيشرح إزاي تبني نسخة الماك وتوزّعها على الموظفين **كتطبيق تشغيل بس — من غير سورس كود**.

> ⚠️ مهم: ملف الماك (`.dmg`) **لازم يتبني على جهاز Mac** — مينفعش يتبني من ويندوز.

---

## 🔒 الفكرة باختصار (مهمة جدًا)

- الـ **zip ده فيه السورس** — هو **ليك إنت بس** عشان تبني منه مرة واحدة.
- اللي بتوزّعه على الموظفين هو ملف **`.dmg`** اللي هيطلع من خطوة البناء.
- لما الموظف يثبّت الـ `.dmg`، الكود بيكون **مقفول جوه أرشيف داخلي (`app.asar`)** → **مفيش أي ملف سورس مكشوف**، مجرد تشغيل بس. (نفس مبدأ نسخة الويندوز بالظبط.)

**باختصار: إنت تبني `.dmg` مرة → تبعت الـ `.dmg` بس للناس → محدش يشوف الكود.**

---

## 🧰 المتطلبات (مرة واحدة)

1. **Node.js** — نزّله من https://nodejs.org (نسخة **LTS**).
   بعد التثبيت افتح **Terminal** واكتب للتأكد:
   ```bash
   node -v
   ```
   لازم يطلع رقم نسخة (مثلاً `v20.x.x`).

2. فك ضغط `Veylmont-CRM-Mac.zip` في أي مكان (مثلاً الـ Desktop).

---

## 📦 بناء ملف التثبيت `.dmg` (اللي هتوزّعه)

1. افتح **Terminal** وادخل على المجلد:
   ```bash
   cd ~/Desktop/"Veylmont CRM Mac"
   ```
2. ثبّت الحزم (مرة واحدة، محتاج إنترنت):
   ```bash
   npm install
   ```
3. ابنِ التطبيق:
   ```bash
   npm run dist:mac
   ```
4. هتلاقي الملف جاهز في مجلد **`dist/`** باسم زي:
   ```
   Veylmont CRM-1.0.0.dmg
   ```
   ✅ ده الملف اللي تبعته للموظفين. الكود جواه مقفول (asar) — مفيش سورس مكشوف.

---

## ▶️ تشغيل الموظف للتطبيق

1. يفتح الـ `.dmg` ويسحب أيقونة **Veylmont CRM** لمجلد **Applications**.
2. يفتحه من **Launchpad** زي أي برنامج.
   هيظهر الـ splash الذهبي ويفتح الـ CRM على طول.

---

## 🔓 أول مرة يفتحه: رسالة "unidentified developer"

التطبيق مش موقّع بشهادة Apple مدفوعة، فأول مرة الماك ممكن يقول:
> "Veylmont CRM" can't be opened because it is from an unidentified developer.

ده طبيعي. الحل (مرة واحدة بس):

**الطريقة أ —** كليك يمين على أيقونة التطبيق → **Open** → في الرسالة اضغط **Open** تاني.

**الطريقة ب —** من **System Settings › Privacy & Security** → انزل لجملة عن "Veylmont CRM" → اضغط **Open Anyway**.

بعد كده هيفتح عادي كل مرة.

---

## ⚡ (للتجربة السريعة عندك إنت بس) تشغيل مباشر بدون بناء

لو عايز تجرّب بسرعة على جهازك من غير ما تبني `.dmg`:
```bash
cd ~/Desktop/"Veylmont CRM Mac"
npm install
npm start
```
> ⚠️ الطريقة دي بتشغّل من السورس — استخدمها للتجربة عندك بس، مش للتوزيع.

---

## 💡 معلومات مهمة

- التطبيق بيفتح **نفس الموقع الحي** (`keelmont.com`) + **نفس الداتابيز** → أي حاجة تتعمل في التطبيق تظهر على الويب والعكس.
- لو الموظف قفل النافذة، التطبيق بيفضل شغّال في **شريط القوائم العلوي** (الإشعارات والصوت بيوصلوا). للخروج نهائي: **Veylmont CRM › Quit**.
- لو النت فصل، هتظهر شاشة "تعذّر الاتصال" بزر إعادة محاولة.

---

## 🔄 (اختياري) بناء تلقائي من GitHub — من غير جهاز ماك

لو معندكش ماك دلوقتي:
1. ارفع مجلد المشروع على repo في GitHub.
2. شغّل الـ workflow اسمه **"Build & release desktop apps"** (جاهز في `.github/workflows`).
3. هيطلعلك الـ `.dmg` جاهز للتحميل من Artifacts.

---

أي مشكلة في أي خطوة، ابعتلي صورة الرسالة وأظبطهالك. 👍
